# Base URL of the EduNexus API. Leave blank in local development: the Vite dev
# server proxies /api to http://127.0.0.1:5055 so requests stay same-origin.
# Set this for deployed builds, e.g. https://edunexus-api-production.up.railway.app
VITE_API_URL=
