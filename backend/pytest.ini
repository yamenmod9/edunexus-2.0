[pytest]
testpaths = tests
pythonpath = .
# The domain has models called TestForm and TestAttempt. Without this, pytest
# tries to collect them as test classes and warns on every run.
python_classes =
